# Sidecar services started alongside the project container. They share the
# project's network namespace, so `localhost:5432` inside the shell hits
# postgres directly. One spec per line; `#` comments allowed.
#
# Built-in shortcuts: redis, postgres, mongo, mysql, mailpit.
# Anything containing `/` or `:` is treated as a raw image reference.
postgres
redis
mailpit
# ghcr.io/foo/bar:1.2.3   # arbitrary image works too
